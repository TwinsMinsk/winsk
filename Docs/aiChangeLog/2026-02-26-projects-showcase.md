# Изменения за 26 Февраля 2026: Витрина кейсов (Фаза 2)

## Что сделано:

1. **Архитектура данных:**
   - Из документа `master-claude-portfolio.md` извлечены все 6 проектов.
   - Разработаны строгие TypeScript-интерфейсы в `src/types/project.ts` (ProjectMeta, Tier, Metric, TechItem).
   - Создан файл данных `src/data/projects.ts` с полными метаданными проектов, категоризацией по Tier'ам (1-3) и функциями фильтрации.

2. **Атомарные UI-компоненты:**
   - `Badge.tsx` — стилизованные теги стека технологий, стилизованные под JSON/terminal.
   - `MetricCard.tsx` — карточки показателей формата `output: value`.
   - `Card.tsx` — базовая структура карточки с поддержкой Glassmorphism-стилей.

3. **Бизнес-компоненты:**
   - Создан `ProjectCard.tsx` — карточка проекта, реагирующая на `tier-2` для применения более крупных Bento-размеров на сетке. Добавлены framer-motion эффекты при наведении.
   - Разработан `ProjectsGrid.tsx` — интерактивная сетка фильтрации проектов. Визуально сохранена иерархия Tier-ов, если показываются все проекты. Активные фильтры обновляются без перезагрузки за счет useState.

4. **Интеграция и верификация:**
   - `ProjectsGrid` добавлен на главную страницу (`app/page.tsx`).
   - Успешная компиляция: `npx tsc --noEmit` пройден без ошибок.

## Следующие шаги
- Фаза 3: Генерация динамических страниц проектов (`/case/[slug]`) с использованием парсинга MDX.
- Создание контентных MDX-файлов для кейсов.
