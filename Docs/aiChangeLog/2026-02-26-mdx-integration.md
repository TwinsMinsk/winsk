# Изменения за 26 Февраля 2026: Интеграция MDX-кейсов (Фаза 3)

## Что сделано:

1. **Инфраструктура MDX:**
   - Настроен плагин `@tailwindcss/typography` для поддержки стилизации Markdown-контента (`prose`). Класс добавлен в `globals.css`.
   - Разработана утилита парсинга `src/lib/mdx.ts` на базе `next-mdx-remote/rsc` (без `parseFrontmatter: true`, чтобы соблюсти Single Source of Truth c `projects.ts`).
   - Создан мощный маппер компонентов `src/components/case/mdx-components.tsx`, преобразующий базовые теги (h2, p, ul) в компоненты с дизайном Portfolio Terminal Theme.

2. **Богатые MDX-компоненты (Rich UI):**
   - `<CaseHero />` — шапка кейса со скриншотом/градиентом.
   - `<SolutionsTable />` — таблица в стиле дашборда с адаптивным дизайном и поддержкой внутреннего HTML парсинга жирного текста.
   - `<Screenshots />` — адаптивная галерея мокапов.
   - `<CodeSnippet />` — компонент вывода кусков кода с UI под стилистику MacOS-терминала.
   - `<ArchDiagram />` — заглушка под графические диаграммы архитектуры.
   - `<CaseNavigation />` — умная перелинковка "Следующий/Предыдущий кейс" в самом низу статьи.
   - `<CaseCTA />` — призыв к обсуждению проекта с кнопками перехода в контакты/Telegram.

3. **Динамический роутинг:**
   - Создан `app/case/[slug]/page.tsx` с реализацией `generateStaticParams()` для полной генерации статики (SSG). Страницы будут летать!
   - Интегрирована автоматическая генерация мета-данных (`generateMetadata`).

4. **Миграция контента:**
   - Написаны 6 `.mdx` файлов в директории `src/content/projects/` на основе структуры из `master-claude-portfolio.md`. Все кейсы Tier 1-3 получили уникальное оформление.

5. **Тестирование:**
   - Устранены ошибки компиляции `SolutionsTable.tsx`. Запуск `npx tsc --noEmit` прошел со 100% успехом (0 errors).

Сайт полностью работоспособен. При клике на карточку проекта открывается красивая страница кейса с детальным техническим разбором.
