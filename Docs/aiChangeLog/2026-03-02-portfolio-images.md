# Портфолио - добавление изображений проектов

**Date:** 2026-03-02
**Description:** Сгенерированы и добавлены мокап-изображения для существующих проектов портфолио.

## Что было сделано:
1. Создана структура папок `public/images/projects/[slug]/`.
2. Сгенерированы красивые UI/UX мокапы для карточек проектов (в стиле Dark Mode / Glassmorphism) для замены пустых заглушек-эмодзи:
   - `lms-lucy-nails`
   - `erp-lomuebles`
   - `crm-ya-i-ty`
   - `ai-ecosystem`
   - `site-lomuebles`
   - `prozub-by`
3. Изображения перемещены в папку `public` в формате `.png`.
4. В файле `src/data/projects.ts` расширения файлов `hero.webp` обновлены на `hero.png`.
5. Обновлен компонент `ProjectCard.tsx` — заменены текстовые заглушки на использование компонента `<Image />` из Next.js со стильными эффектами наведения (zoom, градиент).
6. Добавлен индикатор внешних ссылок (иконка со стрелкой) для проектов, у которых указан `productionUrl`.

## Проверочные шаги (Verification):
- `npx tsc --noEmit` — Ошибок нет.
- Сервер успешно подхватил новые изображения.

## Отклонения от PRD:
- Использован формат `.png` вместо `.webp` ввиду ограничений генератора изображений, это никак не влияет на работоспособность (Next.js умеет оптимизировать их налету или оставлять как статику).
