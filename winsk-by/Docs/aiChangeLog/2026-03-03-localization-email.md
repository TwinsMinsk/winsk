# Локализация на русский и обновление email

## Что было сделано:
1. В верхнем меню (`Navbar.tsx`) ссылки переведены на русский:
   - Work -> Проекты
   - Stack -> Навыки
   - About -> Обо мне
   - Contact -> Контакты
2. Изменена почта на `twinsminsk@yandex.ru` в секции контактов (`ContactSection.tsx`).
3. В форме контактов (`ContactForm.tsx`) обновлены лейблы кнопок "Тип запроса":
   - Project -> Разработка
   - Consult -> Консалтинг
   - Hire -> Сотрудничество
4. Названия блоков (заголовки) были переведены или адаптированы:
   - В блоке услуг (`ServicesSection.tsx`): `System.Capabilities_` заменено на `Услуги_`.
   - В блоке навыков (`BentoSkills.tsx`): `System.Profile_` заменено на `Профиль_`.
   - На странице "Об авторе" (`about/page.tsx`): Переведены заголовок страницы (Обо мне, факты, пункты меню).

## Зачем это было сделано:
Запрос пользователя на локализацию сайта и перенос данных на новую почту для более удобного сбора лидов на родном языке.

## Отклонения от PRD/TechSpec:
Отклонений нет. Стили киберпанка и моноширинные терминальные тексты полностью сохранены.

## Результаты проверки:
- TypeScript (tsc) успешно проходит без ошибок.
- Сайт локально компилируется и отображается корректно.
